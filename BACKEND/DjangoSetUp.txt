create virtual env for our library files:
            python -m venv env

cd into the scripts and run activate.bat, batch file. this will return cursor with (venv)

then run:
    pip install django djangorestframework django-cors-headers

    this will install django, rest API framework, and cross origin for allowing specific IPs


to start server:
    python manage.py runserver
